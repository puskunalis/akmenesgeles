import { DeleteIcon  } from "@chakra-ui/icons";
import { Box, Heading, HStack, IconButton, Text } from "@chakra-ui/react";
import { deleteItem, selectAllItems } from "../../../state/items/ItemsSlice";
import { store } from "../../../state/store";
import { useSelector } from "react-redux";

export interface adminItemsList{

}

export function adminItems(props: adminItemsList) {
    
    return (
        <></>
    );
}