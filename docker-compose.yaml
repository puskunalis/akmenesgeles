version: "3.1"

services:
  db:
    image: postgres:15.2-alpine3.17
    restart: always
    ports:
      - 5433:5432
    environment:
      POSTGRES_DB: flowershop
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
